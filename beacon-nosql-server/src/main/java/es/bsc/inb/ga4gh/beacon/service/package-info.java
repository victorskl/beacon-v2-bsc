/**
 * NoSQL services that implement data queries logic.
 * 
 * Each service returns beacon results of just one type.
 * The bean name (@Named) defines the beacon type queried by the service. 
 */

package es.bsc.inb.ga4gh.beacon.service;
